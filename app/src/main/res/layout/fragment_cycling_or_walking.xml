<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"

    android:paddingBottom="?attr/actionBarSize">

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fillViewport="true"
        android:orientation="vertical">

        <LinearLayout style="@style/FormContainer">

            <LinearLayout style="@style/FormBox">

                <TextView
                    style="@style/Header1"
                    android:text="@string/cycling_or_walking" />

                <View style="@style/FormLine" />

                <TextView
                    style="@style/Header2"
                    android:text="@string/distance_cycled_or_walked" />

                <EditText
                    android:id="@+id/distance"
                    style="@style/Input"
                    android:hint="@string/distance_in_km" />

            </LinearLayout>

            <Space
                android:layout_width="0dp"
                android:layout_height="0dp"
                android:layout_weight="1"></Space>

            <LinearLayout style="@style/FormNav">

                <Button
                    android:id="@+id/back"
                    style="@style/FormButton"
                    android:text="@string/back" />

                <Button
                    android:id="@+id/submit"
                    style="@style/FormButton"
                    android:text="@string/submit" />
            </LinearLayout>

        </LinearLayout>
    </ScrollView>

</androidx.constraintlayout.widget.ConstraintLayout>